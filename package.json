{
  "name": "frontend",
  "version": "1.0.0",
  "description": "Student project for the InfoVis class in WS20/21",
  "main": "index.js",
  "scripts": {
    "scss": "node-sass --watch assets/scss -o assets/css",
    "server": "http-server",
    "start": "npm install && concurrently --kill-others \"npm run scss\" \"npm run server\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-sass": "^5.0.0"
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "http-server": "^0.12.3"
  }
}
